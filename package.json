{
  "private": true,
  "scripts": {
    "test": "kbb --backend sci --classpath src:test -e \"(require '[clojure.test :as t] 'quicknode.main-test) (let [r (t/run-tests 'quicknode.main-test)] (.exit js/process (if (pos? (+ (:fail r) (:error r))) 1 0)))\""
  }
}
